{
  "entities": [{"id": "box", "tags": ["sized"]}],
  "rules": [{
    "from": "#sized",
    "where": { "var": "entities" },
    "do": {
      "mut": [{"target": { "flexWidth": true }, "value": {"value": "not a float", "percent": true}}]
    }
  }]
}
