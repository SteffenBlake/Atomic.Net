{
  "entities": [
    {
      "id": "ui-element",
      "tags": ["test"]
    }
  ],
  "rules": [{
    "from": "#test",
    "where": { "var": "entities" },
    "do": {
      "mut": [
        {
          "target": { "transform": { "anchor": "x" } },
          "value": 0.5
        }
      ]
    }
  }]
}
